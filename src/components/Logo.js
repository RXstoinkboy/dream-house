import React from 'react'
import styles from './styles/Logo.module.css'

const Logo = () => {
    return (
        <div className={styles.wrapper}>
          logo  
        </div>
    );
};

export default Logo;