import React from 'react'
import styles from './styles/HeaderTitle.module.scss'

const HeaderTitle = () => {
    return (
        <h1 className={styles.title}>This is where your home is</h1>
    );
};

export default HeaderTitle;